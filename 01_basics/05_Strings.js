const name = "Abinash";
const repoCount = 50;
// console.log(name + repoCount);  older way

console.log(`My name is ${name} and my repoCount is ${repoCount}`); //an updated way

const new_name = new String(`Abinosh`);
console.log(new_name[4]);
console.log(new_name.__proto__);
console.log(new_name.toUpperCase());
console.log(new_name.charAt(6));
console.log(new_name.indexOf(""));
const new_number = new_name.substring(0,3);
console.log(new_number);
const alaganumber = new_name.slice(-1,-3);
console.log(alaganumber);

const newStringname=  "  Abinash  ";
console.log(newStringname.trim());

const url = "abinash .com";
url.replace('','-');
console.log(url);
 // string to array based on any condition say Abi-nas-h [based on "-"]








